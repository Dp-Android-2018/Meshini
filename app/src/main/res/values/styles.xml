<resources>

    <!-- Base application theme. -->


    <style name="CustomDialog" parent="android:Theme.Holo.Dialog">
        <item name="android:windowMinWidthMajor">100%</item>
        <item name="android:windowMinWidthMinor">100%</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowAnimationStyle">@style/DialogAnimation</item>
    </style>

    <style name="DialogAnimation">
        <item name="android:windowEnterAnimation">@anim/pop_up</item>
        <item name="android:windowExitAnimation">@anim/slide_out_down</item>
    </style>


    <style name="RatingBar" parent="Theme.AppCompat">
        <item name="colorControlNormal">@color/text_color_black</item>
        <item name="colorControlActivated">@color/yellow_color</item>
    </style>
    
    <style name="bottom_nav" parent="Widget.Design.BottomNavigationView">
        <item name="enforceTextAppearance">false</item>
    </style>

    <style name="NavigationTheme" parent="Theme.AppCompat">
        <item name="android:textSize">15sp</item>
        <item name="android:gravity">center</item>
    </style>

</resources>
